"""Shared Phase 3 full-profile-workflow test package (task 06A).

Test cases live in non-``test_`` modules and are collected only through
``tests/integration/test_full_profile_workflow.py`` via ``pytest_plugins`` so
the historical focused path still runs the full suite without double collection.
"""
